name: Workflow 3.2
on:
schedule:
  - cron: '*/10 * * * *'
  - cron: '0 0 1 * *'
  jobs:
  say_hello:
  runs-on:
  windows-latest steps:
- name: Afficher le message Hello, world!
  run: echo "Hello, world!"
